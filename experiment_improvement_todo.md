# GaussianFusion 插帧改进 Todo List

> 目标：改善大尺度、快速运动场景下的插帧质量。
> 当前已解决：运动物体淡入淡出（ghosting）方向问题。

---

## 1. 明确淡入淡出方向修正（前提，不可破坏）✅ 已完成

### 问题回顾
曾因 `flow_warp` 传入 forward flow 导致 warp 方向反了：
- 运动物体特征没有对齐到中间时刻，反而被推向相反方向
- 表现为「背景搬到物体上、物体变透明」

### 当前修正方式
`flow_warp(x, f)` 的语义是 `y(p) = x(p + f(p))`，即 **f 必须是 backward flow**（从输出坐标指向输入坐标）。

模型的 `flow_01`（0→N）和 `flow_10`（N→0）都是 **forward flow**（源→目标），所以必须取负：

```python
flow_0t = -t * flow_01          # τ → 帧0
flow_1t = -(1.0 - t) * flow_10  # τ → 帧N
```

### 约束
后续所有新 warp 调用（残差流修正、遮挡检测）都必须遵守 backward flow 约定，否则 ghosting 会复发。

---

## 2. 残差流修正（学习式 refine）🔲 待实现

### 目标
修正线性假设 `F_0→τ ≈ τ·F_0→N` 在以下场景的误差：
- 非匀速（加速/减速）运动
- 大位移（超出训练分布）
- 透视变化（靠近/远离相机）
- 转向/弧线运动

### 方案
在 warp 之后加轻量 refine 卷积，预测残差流 `Δflow` 修正对齐误差：
- 输入：两个 warped 特征 + τ map
- 输出：2 通道残差流
- 用修正后的流重新 warp（可迭代 1~2 次）
- 训练监督：GT 中间帧的像素/感知损失

### 约束
- 2 帧输入，光流冻结，残差流靠特征 + GT 监督学习
- 新增模块参数用默认值初始化，旧 checkpoint `strict=False` 加载兼容

---

## 3. 遮挡/无效区检测 🔲 待实现

### 目标
识别线性假设失效的区域（`fb_err` 大、遮挡、出现新物体），降低这些区域的 warp 权重，避免错误对齐特征强行融合。

### 方案
基于已有的前向后向一致性误差：
```python
fb_err = ‖flow_01 + warp(flow_10, flow_0t)‖
```
把遮挡置信度显式并入融合权重（当前 `occ_fusion` 已做基础版本，可增强为可学习或更细粒度）。

---

## 4. 更换光流模型 → WAFT 🔲 待评估

> WAFT: Warping-Alone Field Transforms for Optical Flow

### 为什么更适合大尺度运动
- 基于多尺度 warp 场，对大幅位移的跨尺度匹配更强
- 相比 RAFT 迭代式精化，WAFT 的 warping 变换在大位移下有更好的全局一致性

### 方案
1. 调研 WAFT 是否有公开权重/接口
2. 封装成与 `SeaRAFT` 相同的接口：`forward(im1, im2) -> flow [B, 2, H, W]`
3. 通过 `flow_model` 配置切换
4. 保持 backward flow 约定不变（见第 1 项）

---

## 优先级排序

| 优先级 | 项 | 需重训? | 预期收益 |
|:---:|------|:---:|------|
| P0 | 残差流修正 | ✅ 是 | 直接改善非匀速/大位移 |
| P1 | 遮挡/无效区检测 | ✅ 是（若可学习） | 避免错误融合 |
| P2 | WAFT 光流替换 | ❌ 否（替换即用） | 大尺度运动光流更准 |
